import React from 'react';
import '../styles/style.css'

export default class EmptySectionPlaceholder extends React.Component {
    render() {
        return (
            <div className="empty-placeholder">
                {/* Пустовато, не правда ли... */}
            </div>
        )
    }
}